import { trpc } from '../../lib/trpc';

export default function GroceryList(): React.ReactElement {
  // const { data } = trpc.grocery.useQuery();

  return (
    <div>
      <h1>Grocery</h1>
      {/* Implement UI here */}
    </div>
  );
}
