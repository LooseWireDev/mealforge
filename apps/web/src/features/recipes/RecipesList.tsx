import { trpc } from '../../lib/trpc';

export default function RecipesList(): React.ReactElement {
  // const { data } = trpc.recipes.useQuery();

  return (
    <div>
      <h1>Recipes</h1>
      {/* Implement UI here */}
    </div>
  );
}
