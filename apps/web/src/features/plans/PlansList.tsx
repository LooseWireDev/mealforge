import { trpc } from '../../lib/trpc';

export default function PlansList(): React.ReactElement {
  // const { data } = trpc.plans.useQuery();

  return (
    <div>
      <h1>Plans</h1>
      {/* Implement UI here */}
    </div>
  );
}
